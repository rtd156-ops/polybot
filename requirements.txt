# --- Runtime (V1 analysis + paper) ---
requests>=2.31,<3
PyYAML>=6.0,<7
python-dotenv>=1.0,<2

# --- Dev / tests ---
pytest>=8.0,<9

# --- Live execution (OPTIONAL, not required for V1 analysis/paper) ---
# Uncomment only when enabling live trading on a secured host.
# py-clob-client>=0.17
# web3>=6.0
# websocket-client>=1.6      # only for the opt-in WebSocket order-book stream
